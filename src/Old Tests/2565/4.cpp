#include <iostream>
using namespace std;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(0);

	pair <int, int> p;

	long long num;
	int i;

	cin >> num;

	for (i = 2; i <= num; i++) {
		float y = num / i;
		int z = y;
		if (z == y) {
			for (i = 2; i< )
		}
	}
	return 0;
}