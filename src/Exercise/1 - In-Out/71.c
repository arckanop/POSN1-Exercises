#include <stdio.h>
int main() {
    int age; // Creates a variable to store the age
    printf("Enter your age (Positive Integer): "); // Asks the user to enter their age as a positive integer
    scanf("%d", &age); printf("Your age is %d years old.", age); // Tells the user how old they are
    return 0;
}
